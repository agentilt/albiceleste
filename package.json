{
  "name": "albiceleste-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "typecheck": "turbo run typecheck",
    "web": "npm run dev --workspace=web"
  },
  "devDependencies": {
    "tsx": "^4.23.13",
    "turbo": "^2.10.12",
    "typescript": "^5.9.3"
  },
  "packageManager": "npm@11.17.0",
  "engines": {
    "node": ">=22"
  }
}
